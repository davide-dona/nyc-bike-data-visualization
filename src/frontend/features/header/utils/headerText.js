// Centralized display copy for the app header; nav and filter labels stay in their config arrays (PAGES, filterOptions.js).
export const HEADER_TEXT = {
    dateWindow: {
        from: 'FROM',
        to: 'TO',
        selectStart: 'Select start',
        selectEnd: 'Select end',
    },
    lockHint: 'Class filters are locked while comparison surfaces are active. Remove all surfaces or press Reset to unlock them.',
}
