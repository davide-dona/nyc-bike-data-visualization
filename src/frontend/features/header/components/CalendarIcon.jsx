/**
 * Calendar glyph used inside the date window picker inputs.
 * @returns The rendered Font Awesome calendar icon.
 */
export default function CalendarIcon() {
    return <i className="fa-solid fa-calendar-days" aria-hidden="true" />
}
