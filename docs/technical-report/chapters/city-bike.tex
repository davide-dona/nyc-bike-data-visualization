\section{City Bike Dataset}
For this project, our main dataset is the \href{https://www.citibikenyc.com/system-data}{Citi Bike system dataset}, a public repository tracking New York City bike trips since 2013.

\subsection{Dataset Structure}
Released monthly as open-data CSV files, these records log every completed ride, with high-volume months split across multiple files.
Each row represents a single trip, offering detailed attributes that characterize the journey.

\subsubsection{Original Structure}
The original dataset includes the following columns:
\begin{itemize}
    \item \textbf{Ride ID}: A unique identifier for each bike trip.
    \item \textbf{Rideable Type}: The type of bike used for the trip (``classic bike'' or ``electric bike'').
    \item \textbf{Started At}: A timestamp indicating when the trip started.
    \item \textbf{Ended At}: A timestamp indicating when the trip ended.
    \item \textbf{Start Info}: Information about the station and location where the trip started, structured as:
          \begin{itemize}
              \item \textbf{Station Name}: The name of the starting station.
              \item \textbf{Station ID}: The identifier of the starting station.
              \item \textbf{Latitude}: The latitude coordinate of the starting station.
              \item \textbf{Longitude}: The longitude coordinate of the starting station.
          \end{itemize}
    \item \textbf{End Info}: Information about the destination station, structured equally to Start Info.
    \item \textbf{User Type}: The type of user (``member'' or ``casual'').
\end{itemize}

\subsubsection{Extracted Features}
Additional per-trip features were derived to support the analysis:
\begin{itemize}
    \item \textbf{Trip Duration}: The duration of the trip in seconds, calculated as $\text{Ended At} - \text{Started At}$.
    \item \textbf{Trip Distance}: The distance between the start and end stations, computed using the Haversine formula and multiplied by a circuity factor of $1.3$ to better approximate real routes.
    \item \textbf{Day of the Week}: The weekday extracted from the end timestamp, which is used to partition the data since trips are published based on when they ended.
    \item \textbf{Hour of the Day}: The hour extracted from the end timestamp, for the same reason.
\end{itemize}

Beyond these per-trip features, two aggregated datasets are derived from the cleaned trip data to support station- and route-level analysis: [Name of Dataset 1] and [Name of Dataset 2].
\begin{itemize}
    \item \textbf{Station Popularity}: for each station and time period, the number of trips starting and ending at that station, obtained by grouping trips by station.
    \item \textbf{Trips Flow}: for each pair of stations and time period, the number of trips travelling between them, obtained by grouping trips by station pair.
\end{itemize}

\subsection{Analysis and Insights}
To ensure transparency and reproducibility, the entire analysis is supported by \href{https://github.com/davide-dona/nyc-bike-data-visualization/blob/main/src/notebooks/analysis.ipynb}{\underline{Jupyter notebook}} within our repository.
The exploratory analysis focuses on temporal, spatial, and behavioural patterns in bike usage.

\paragraph{Temporal Analysis}
Temporal analysis shows clear variations in demand depending on the hour of the day and the day of the week.
Trips are concentrated during commuting hours, while weekends show different usage patterns associated with leisure activities.

\begin{figure}[H]
    \centering
    \includegraphics[width=.8\textwidth]{../media/rides_time.png}
    \caption{Distribution of trips by hour of the day and day of the week.}
    \label{fig:rides_time}
\end{figure}

\paragraph{Spatial Analysis}
Spatial analysis highlights the distribution of trips across the city.
Station popularity and trip flows reveal that demand is concentrated around Manhattan and other high-density areas, with some stations acting as important origins.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{../media/rides_stations.png}
    \caption{Distribution of trips by station of origin, showed through an heatmap.}
    \label{fig:rides_stations}
\end{figure}

\paragraph{User/Bike Type Behaviour Analysis}
As shown in Figure~\ref{fig:rides_type}, casual riders lean much more heavily on electric bikes than members do.
Furthermore, casual riders take longer trips on both bike types, whereas members maintain a steady, short duration regardless of the bike they choose. 
\begin{figure}[H]
    \centering
    \includegraphics[width=.8\textwidth]{../media/rides_type.png}
    \caption{Distribution of trips by user type based on shares of rides and trip length.}
    \label{fig:rides_type}
\end{figure}

\paragraph{Trip Duration and Distance}
Trip duration and distance distributions provide additional insight into mobility behaviour.
Most trips are relatively short, while longer rides are less frequent and often correspond to recreational usage.
The distributions of trip duration and distance shows some anomalies that we are describing in the following section.

\begin{figure}[H]
    \centering
    \begin{minipage}[t]{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../media/rides_duration.png}
        \caption{Distribution of trip durations.}
        \label{fig:rides_duration}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.45\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../media/rides_distance.png}
        \caption{Distribution of trip straight-line distances.}
        \label{fig:rides_distance}
    \end{minipage}
\end{figure}

These analyses justify our dashboard proposal, as they instantly reveal the clear relationships across temporal patterns, station-level dynamics, and user flows.

\subsection{Data Quality}
Various data quality issues were encountered during the analysis of the City Bike dataset.

\paragraph{Different Structure}
During the analysis, we noticed that older datasets have a different structure compared to newer ones.
Older datasets may miss columns introduced in newer versions, such as "Rideable Type" or station-related fields, while including deprecated attributes such as Gender, Birth Year, and Bike ID.

This inconsistency required adjustments in the preprocessing pipeline to ensure that all monthly files produced compatible features.

\paragraph{Missing Values and Inconsistencies}

\begin{comment}
TODO:
Discussion of any data quality issues, such as missing values, outliers, noise, bias, or inconsistencies, and how they were addressed in the analysis.
OUTLIERS: some trips have too long durations (may be caused by user not docking the bike properly, keeping the bike for a long time, but surely not a real trip), or too short (may be caused by user not docking the bike properly, but surely not a real trip)
some trips have the same start and end station (could be possible, but it might screw up the analysis, need to be filtered out)

BIAS: 1) we are considering only the data from the Citi Bike system, which may not be representative of the entire population of bike users in New York City.
2) To compute the length of the trips, we are using the Haversine formula between start station and end station. This is just an approximation.

ISSUES:
Two UUID (short name and station id) were used to uniquely identify stations, but they are not consistent across the dataset, so we had to use the short name as the unique identifier for stations.
Live data has inactive stations that need to be filtered out, and bike counts aren't always perfect

This are just the ones that come to my mind as of now, but IDK
\end{comment}

Several data quality issues were considered during preprocessing.

The trip records contain a small number of outliers in terms of duration. Extremely short trips are often associated with docking or rental issues, while very long trips typically correspond to users keeping a bike for an extended period before returning it rather than continuous riding. Since these records do not accurately represent typical cycling activity, they are removed using reasonable duration thresholds. Similarly, trips starting and ending at the same station are excluded from distance-based analyses, as they likely correspond to round trips or erroneous records and would otherwise bias the results.

The dataset also presents some inherent sources of bias. First, it only includes trips performed through the Citi Bike system and therefore is not representative of all bicycle traffic in New York City. Second, trip distances are estimated using the Haversine formula applied to the start and end station coordinates. As this computes the great-circle distance between the two stations rather than the actual route taken, the reported distances should be regarded as approximations.

Station identifiers require particular attention. Although each station is associated with both a station ID and a public short name, these identifiers are not always consistent across different datasets and years. Consequently, stations are uniquely identified using the normalized short name.