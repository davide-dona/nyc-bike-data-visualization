\section{Visualization}\label{sec:visualization}
\begin{comment}
The following chapter will describe how the data is visualized in the web application. 
It should have the following subsections:
- Overview of the web page
- General implementation details like tailwind, react external libraries, etc.
- Choice of hierarchy of the page, and why we choose to have the map page at top and how each page is connected to each other. High level overview of the pages and general design
- For each of the following components, describe what plot was used, why those, what statistics were precomputed, what assumptions does if any, how it interacts, and how an user can understand the plot and the meaning, each plot helped by a figure/screenshot, each should be paired with its own challenges throughout the development of the project and how we solved them:
    - Header filters (this is particular, its maing purpose is to enforce the exploratory nature of the web application, and to allow the user to filter the data and see how the plots change accordingly)
    - Temporal page
    - Each map layer has its own subsection
    - Footprint page
    - Weather page
- User experience trhough explanatiory hints and why we did that, and how we aim to interact to the target user
\end{comment}
